{
  "//": "TypeScript in opt-in checkJs mode — add `// @ts-check` on modules where real type-checking pays off (pure helpers, small modules). popup.js and the content scripts stay opt-out for now because typing every DOM-element access via el('id').value would need either JSDoc generics or a TS migration, neither of which is on this sprint's ticket. Run as `npm run typecheck`.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "checkJs": false,
    "allowJs": true,
    "noEmit": true,
    "strict": false,
    "noImplicitAny": false,
    "noFallthroughCasesInSwitch": true,
    "noImplicitOverride": true,
    "noUncheckedIndexedAccess": false,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "types": ["chrome"],
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["extension/**/*.js"],
  "exclude": ["node_modules", "bin", "logs", "host", "tests"]
}
